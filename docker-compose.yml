version: "3.9"

services:
  frontend:
    build:
      context: ./frontend
    ports:
      - "4173:80"
    depends_on:
      - backend

  backend:
    build:
      context: ./backend
    env_file:
      - ./backend/.env
    ports:
      - "8080:8080"
